library(swirl)
install_from_swirl("Getting and cleaning data")
install_from_swirl("Getting and Cleaning Data")
setwd("C:/Users/Keitaro/Git/RepData_PeerAssessment1")
rawdata <- read.table(unz("activity.zip", "activity.csv"), header = TRUE, sep = ",")
head(rawdata)
str(rawdata)
cleandata <- rawdata
cleandata$date <- as.date(cleandata$date)
cleandata$date <- as.Date(cleandata$date)
str(cleandata)
library(dplyr)
by_day <- group_by(cleandata, date)
summarize(by_day, sum(steps))
by_date <- group_by(cleandata, date)
by_date_steps <- summarize(by_day, sum(steps))
hist(by_date_steps)
hist(by_date_steps)
by_date_steps
hist(by_date_steps$`sum(steps)`)
by_date <- group_by(cleandata, date)
by_date_steps <- summarize(by_day, sum(steps))
by_date_steps <- rename(by_date_steps, totalsteps = `sum(steps)`)
hist(by_date_steps$`sum(steps)`)
hist(by_date_steps$totalsteps)
mean(by_date_steps$totalsteps)
mean(by_date_steps$totalsteps, na.rm = TRUE)
median(by_date_steps$totalsteps)
median(by_date_steps$totalsteps, na.rm = TRUE)
?hist
hist(by_date_steps$totalsteps, main = paste("Histogram of", xname), xlab = "Total number of steps taken each day")
hist(by_date_steps$totalsteps, main = paste("Histogram of", xlab), xlab = "Total number of steps taken each day")
hist(by_date_steps$totalsteps, main = paste("Histogram of total number of steps taken each day"), xlab = "Total number of steps taken each day")
mean(by_date_steps$totalsteps, na.rm = TRUE)
by_interval <- group_by(cleandata, interval)
by_interval_steps <- summarize(by_interval, mean(steps))
by_interval_steps
by_interval_steps <- summarize(by_interval, mean(steps, na.rm = TRUE))
by_interval_steps
by_interval_steps <- rename(by_interval_steps, averagesteps = `mean(steps, na.rm = TRUE)`)
by_interval_steps
plot(by_interval_steps)
plot(by_interval_steps, type = "l")
max(by_interval_steps$averagesteps)
summary(by_interval_steps$averagesteps)
summary(by_interval_steps)
maximum <- filter(by_interval_steps, interval == max(interval))
maximum
maximum <- filter(by_interval_steps, interval == max(averagesteps))
maximum
maximum <- filter(by_interval_steps, interval == max(by_interval_steps$averagesteps))
maximum
filter(by_interval_steps, interval == max(by_interval_steps$averagesteps))
filter(by_interval_steps, interval >= 200)
filter(by_interval_steps, averagesteps >= 200)
filter(by_interval_steps, averagesteps == max(averagesteps))
head(cleandata)
sum(is.na(cleandata$steps))
plot(cleandata)
?plot
plot(cleandata$date, cleandata$steps)
plot(cleandata$interval, cleandata$steps)
by_interval_steps
by_date_steps
nadata <- filter(cleandata, step == "NA")
head(cleandata)
nadata <- filter(cleandata, steps == "NA")
nonnadata <- filter(cleandata, steps != "NA")
head(nadata)
nadata
nadata <- filter(cleandata, steps == NA)
nadata
nadata <- cleandata[cleandata$steps == "NA"]
nadata <- cleandata[cleandata$steps == "NA",]
head(nadata)
nadata <- filter(cleandata, steps =="NA")
nadata
head(cleandata)
nadata <- filter(cleandata, steps == NA)
head(cleandata)
nadata
nadata <- cleandata[is.na(cleandata),]
nadata
nadata$steps <- interval
head(nadata)
summary(by_interval_steps)
by_interval_steps
nadata <- mutate(nadata, steps = by_interval_steps)
